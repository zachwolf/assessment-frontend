import React from "react";
import Router from "./component/router";
import Layout from "./ui/layout";

const App = () => {
    return (
        <Layout>
            <Router />
        </Layout>
    );
};

export default App;
